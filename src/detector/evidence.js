/**
 * Evidence helpers (scaffold for later detectors).
 */

/**
 * @param {string} source
 * @param {string} detail
 * @returns {{ source: string, detail: string }}
 */
export function createEvidence(source, detail) {
  return { source, detail };
}
